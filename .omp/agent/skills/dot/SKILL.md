---
name: dot
description: Commit dotfiles (bare repo) — same as skill://commit but with custom git command
hide: true
---

Execute skill://commit, but replace `git` with `git --git-dir ~/.cfg --work-tree ~`.
