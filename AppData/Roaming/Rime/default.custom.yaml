patch:
  schema_list:
    - { schema: double_pinyin }

  switcher/fold_options: false
  ascii_composer/switch_key/Shift_L: nonp

  menu:
    page_size: 7

  key_binder/bindings:
    - { when: paging, accept: comma, send: Page_Up }
    - { when: has_menu, accept: period, send: Page_Down }
